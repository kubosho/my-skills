---
status: todo
---

# 仕様名

<!--
各見出し以下のHTMLコメントは、AIエージェント向けの生成指示となる。
設計判断は適用先のネガティブ要件または技術的制約に置き、理由は判断の直下に入れ子リストで添える。
該当する要件がない欄には「なし」と書き、欄を埋めるために要件や理由を作らない。
-->

## 目的

<!--
現状の問題、適用する解、この変更全体で達成する状態の順に書く。流し見で全体が掴める構成にする。
設計判断の一覧は目的に置かない。
-->

現状の問題を書く。

適用する解を書く。

この変更全体で達成する状態を書く。

## 共通要件

<!--
全タスクに適用する判断だけを書く。特定のタスクへの判断は、そのタスク内に置く。
一部のタスクで共有する定義は、定義するタスクに本文と理由を置き、利用するタスクの要件から具体的な見出しと対象の定義を参照する。
依存関係の記載だけで、共有する制約の説明を済ませない。
-->

### ネガティブ要件

<!--
実装しないこと、既存挙動として残すこと、過剰実装になりやすい禁止事項を1行1事項で書く。
禁止事項への違反候補は、変更に問題がないかを人間が最終判定する。タスク内のネガティブ要件も同じ扱いとする。
-->

- 全タスクで実装しないこと、変更してはいけないことを書く
  - その理由を書く

### 技術的制約

<!--
全タスクで守るべき構造、値、方式を1行1事項で書く。値は具体的に定める。
-->

- 全タスクに適用する技術的制約を書く
  - その理由を書く

## スコープ上限

<!--
具体的な数値に置き換える。変更量や総行数が目安を大きく超えたら、変更に問題がないかを人間が最終判定する。
-->

- **変更量の目安**：N ファイル前後
- **総行数の目安**：N 行以内

## タスク

<!--
各 AC-N の節が実装、確認、進捗管理の単位で、配下の箇条書きが個々の受け入れ基準となる。
タスク名は見出しだけで管理し、末尾にタスク一覧を作らない。
状態は「未着手」「進行中」「完了」を使う。タスク内の状態行を更新する。
依存は、完了が必要なタスクの AC-N を列挙し、依存がなければ「なし」と書く。
本番反映や動作確認などが開始に必要なら、依存の次に「開始条件：...」を追加する。
その手順と理由はタスクの技術的制約に置く。依存タスクの完了だけで開始条件を満たした扱いにしない。
タスクを追加するときも見出し階層と各欄の記述ルールを揃える。
-->

### AC-1：タスク名

状態：未着手
依存：なし

<!--
達成する状態を段落の冒頭で述べ、現状の何を変えるか、仕様全体の目的にどうつながるかを続ける。
見出しの言い換えや受け入れ基準の羅列ではなく、変更の意味を説明する。
説明と確認条件は役割が異なるので、同じ対象に触れること自体を重複として削らない。
-->

このタスクで達成する状態を書く。続けて現状からの変更と、仕様全体の目的との関係を説明する。

#### 受け入れ基準

<!--
具体的な入力や操作に対する観測可能な結果を箇条書きで書く。
冒頭の説明にしかない要求や、説明と矛盾する基準を残さない。
実装前に失敗するテストを書ける内容にし、実行可能な挙動がなければ存在チェックを定める。
タスク内の全基準の確認が成功し、diff が共通要件、対象タスクの要件、明示的な参照先に従うことを確認する。
他タスクが保護する既存挙動への違反も見落とさない。
-->

- 具体的な入力や操作を書き、期待される観測可能な結果を書く

#### ネガティブ要件

<!--
このタスクで実装しないこと、変更してはいけない既存挙動を1行1事項で書く。
-->

- このタスクで実装しないこと、変更してはいけないことを書く
  - その理由を書く

#### 技術的制約

<!--
このタスクで守るべき構造、値、方式を1行1事項で書く。値は具体的に定める。
他タスクが利用する定義は、ここに本文と理由を置き、必要なら見出し5で定義名を付ける。
利用側には「AC-1『技術的制約』の『定義名』を使う」のように参照先と対象を明記する。
開始条件があれば、必要な本番反映の順序、動作確認、PR の分離などを理由とともに具体化する。
-->

- このタスクに適用する技術的制約を書く
  - その理由を書く
